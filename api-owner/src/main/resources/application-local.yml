spring:
  config:
    import:
      - classpath:application-domain-local.yml
      - classpath:application-domain-redis-local.yml
      - classpath:application-kafka-local.yml
  application:
    name: WellMeet-Backend
server:
  port: 8081

cors:
  origin: http://localhost:5173
