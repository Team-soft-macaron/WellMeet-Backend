server:
  port: 8761

spring:
  application:
    name: discovery-server

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false  # 개발 환경

management:
  endpoints:
    web:
      exposure:
        include: health,info
