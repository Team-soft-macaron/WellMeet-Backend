spring:
  config:
    import:
      - classpath:dev-secret.yml
      - classpath:application-domain-dev.yml
      - classpath:application-domain-redis-dev.yml
      - classpath:application-kafka-dev.yml

cors:
  origin: ${secret.cors.origin}
