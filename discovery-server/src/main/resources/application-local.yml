spring:
  application:
    name: discovery-server

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false  # 로컬 개발: 빠른 서비스 제거

management:
  endpoints:
    web:
      exposure:
        include: health,info
